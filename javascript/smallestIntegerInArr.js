/*
 * Objective:
 * Find the smallest integer in the array. Given an array
 * of integers your solution should find the smallest integer.
 * 
 */

 /*
  * DNI:
  * 
  * 
  * 
  * 
  */


// Class Definition. Properties belong in the constructor method.
// When the class object is initialized, the constructor method is called. 
  class Me {
      constructor(property) {
          this.name = property;
      }
  }

// Create an object called 'name' based on Me Class.

myPerson = new Me('Eric');
console.log(myPerson.name);


  